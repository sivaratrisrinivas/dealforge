SOURCE: NOT FOUND

NOT FOUND

No single documented example was found that combines both DistributedRunner/DistributedWorker (multi-GPU) and a custom ContainerImage (from_dockerfile_str or from_dockerfile) in one app. The multi-GPU tutorial uses fal.distributed without a custom container; the custom container examples use a single GPU image. You would need to combine both patterns in your own code.
